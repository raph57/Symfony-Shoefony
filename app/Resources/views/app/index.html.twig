{% extends "app/layout/layout.html.twig" %}

{% block title %}{{ parent() }} - Accueil{% endblock %}

{% block content %}
    {% block slide %}
        {{ render(controller('AppBundle:Main:slides')) }}

    {% endblock %}
    <!-- Carousel -->
    {#<div id="carousel-example-generic" class="carousel slide" data-ride="carousel">#}
        {#<!-- Indicators -->#}
        {#<ol class="carousel-indicators">#}
            {#<li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>#}
            {#<li data-target="#carousel-example-generic" data-slide-to="1"></li>#}
        {#</ol>#}

        {#<!-- Wrapper for slides -->#}
        {#<div class="carousel-inner" role="listbox">#}
            {#<div class="item active">#}
                {#<img src="{{ asset('bundles/store/slides/slide-1.jpg') }}" alt="Slide 1">#}
            {#</div>#}
            {#<div class="item">#}
                {#<img src="{{ asset('bundles/store/slides/slide-2.jpg') }}" alt="Slide 2">#}
            {#</div>#}
        {#</div>#}

        {#<!-- Controls -->#}
        {#<a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">#}
            {#<span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>#}
            {#<span class="sr-only">Précédent</span>#}
        {#</a>#}
        {#<a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">#}
            {#<span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>#}
            {#<span class="sr-only">Suivant</span>#}
        {#</a>#}
    {#</div>#}
    <!-- /.carousel -->

    <hr>

    <!-- Title -->
    <div class="row">
        <div class="col-lg-12">
            <h3>Les nouveautés (date la + récente)</h3>
        </div>
    </div>
    <!-- /.row -->

    <!-- Page Features -->
    <div class="row text-center">

        {% for product in products %}
            {{ include('store/partial/_products.html.twig', { 'product': product , 'col': 'col-md-3' }) }}
        {% endfor %}
    </div>


        {#{% block displayProducts %}#}
            {#{{ render(controller(#}
            {#'AppBundle:Main:productsDisplay',#}
            {#{ 'col': 'col-md-3' }#}
            {#)) }}#}
        {#{% endblock %}#}


    {#</div>#}
    <!-- /.row -->

    <hr>

    <!-- Title -->
    <div class="row">
        <div class="col-lg-12">
            <h3>Les plus populaires (le + d'avis) <br>(j'ai fait exprès de mettre un having count >0 car il faut qu'il y ait au moins 1 commentaire)</h3>
        </div>
    </div>
    <!-- /.row -->

    <!-- Page Features -->
    <div class="row text-center">

        {#{% if productsFamous is defined %}#}
    {#{% for key,prod in productsFamous %}#}
        {#{{ include('store/partial/_famousProducts.html.twig', { 'product': product }) }}#}
        {#{% endfor %}#}

        {% block displayFamousProducts %}
            {{ render(controller(
            'AppBundle:Main:productsFamousDisplay',
            { 'col': 'col-md-3' }
            )) }}
        {% endblock %}

{#{% endif %}#}
    </div>

{% endblock %}