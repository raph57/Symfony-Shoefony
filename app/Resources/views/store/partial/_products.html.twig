
{% if product.0 is defined %}
    {% set product=product.0 %}

{% endif %}

<div class="{% if col is defined  and col is not null %} {{col}} {% else %} col-md-4 {% endif %}col-sm-6 hero-feature">
        <div class="thumbnail">
            <img src="{{ asset('img/products/' ~ product.image.url) }}" alt="{{ product.title }}">
            <div class="caption">
                <h3>{{ product.title|title }}</h3>
                <p>{{ product.description }}</p>
                <p>
                    <span class="badge">{{ product.price }} €</span>
                    <a href="{{ path('store_product', { 'id' : product.id, 'slug' : product.slug }) }}" class="btn btn-default">Voir la fiche</a>
                </p>
            </div>
        </div>
    </div>




